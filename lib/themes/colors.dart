// colors.dart
import 'package:flutter/material.dart';

class EventHiveColors {
  // Core Palette
  static const Color primary = Color(0xFF00ADB5);   // Teal
  static const Color secondary = Color(0xFF393E46); // Slate Grey
  static const Color accent = Color(0xFFF08A5D);    // Peach Orange
  static const Color background = Color(0xFFEEEEEE);// Soft Grey
  static const Color text = Color(0xFF222831);      // Dark Charcoal

  // Variants (optional)
  static const Color primaryLight = Color(0xFF5ED7DB);
  static const Color secondaryLight = Color(0xFF6D737A);
  static const Color accentLight = Color(0xFFF5A880);
}
